<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.usedcarsstudentproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".YakitActivity"></activity>
        <activity android:name=".MotorPerformansActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".KullaniciBilgileriBuyukResimActivity" />
        <activity android:name=".UserUpdateActivity" />
        <activity android:name=".IlanTuruActivity" />
        <activity android:name=".IlanResimlerActivity" />
        <activity android:name=".IlanlarimActivity" />
        <activity android:name=".IlanlarActivity" />
        <activity android:name=".IlanDetayActivity" />
        <activity android:name=".IlanBilgileriActivity" />
        <activity android:name=".AracBilgileriActivity" />
        <activity android:name=".AdresBilgileriActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>